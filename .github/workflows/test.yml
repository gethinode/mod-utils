name: Test
on:
  workflow_dispatch:
  push:
    tags:
      - v*
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    uses: gethinode/.github/.github/workflows/test.yml@test/no-hoisted
    with:
      package-manager: pnpm
    secrets: inherit
